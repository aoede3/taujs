---
'@taujs/server': minor
---

Emit an honest project-relative `definitionLocation` for services defined with `defineService`, with explicit unknown provenance when no bounded location is available.
