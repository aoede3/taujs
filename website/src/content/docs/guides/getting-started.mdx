---
title: Getting Started with τjs
description: Create a Fastify-hosted React, Vue or Solid application with declared routes and response orchestration.
---

::::note[Project status]
τjs is in active development. Expect breaking changes on the path to v1, and check the release notes when upgrading.
::::

τjs registers application routes from `taujs.config.ts` as real Fastify routes. After Fastify dispatches one of those routes, τjs coordinates its policy, data and rendering before handing the response to the React, Vue or Solid renderer selected by the application.


::::caution[Prerequisites]
**Node `^20.19.0 || >=22.12.0`**, inherited from Vite 8. Check with `node --version`.

**New projects** generated with `@taujs/create-taujs` ship with Vite and its renderer plugin already
pinned. There is nothing else to install.

**Existing projects** adding τjs should satisfy peers as follows:

| Package         | Peer requirement              |
| --------------- | ----------------------------- |
| `@taujs/server` | `vite ^8.2.1`                 |
| `@taujs/react`  | `@vitejs/plugin-react ^5.2.0` |
| `@taujs/vue`    | `@vitejs/plugin-vue ^6.0.3`   |
| `@taujs/solid`  | `vite-plugin-solid ^2.11.11`  |
::::

## Create a project

`@taujs/create-taujs` scaffolds a working application and asks which framework and package manager to use:

```bash
npx @taujs/create-taujs my-app
cd my-app
npm run dev
```

If you selected another package manager, use the command printed by the scaffolder. You can also open the [StackBlitz starter](https://stackblitz.com/edit/taujs-starter?file=README.md).

The generated project includes:

- `taujs.config.ts`, containing one application with its renderer and routes
- server and client entries for the selected framework
- an SSR route and a streaming route
- a small service registry and example service
- development, build and production scripts

## The application contract

Each application declares one renderer and any routes whose responses the server should orchestrate. A declared route chooses `ssr` or `streaming`; hydration is a separate decision.

```ts
{
  appId: "main",
  entryPoint: "",
  renderer: reactRenderer({ project: "./tsconfig.json" }),
  routes: [
    {
      path: "/",
      attr: {
        render: "ssr",
        hydrate: true,
      },
    },
    {
      path: "/products/:id",
      attr: {
        render: "streaming",
        hydrate: true,
      },
    },
  ],
}
```

The same application shape works with `vueRenderer()` or `solidRenderer()`. See the [configuration reference](/reference/taujs-config/) for the complete contract.

A route can also declare head metadata, critical data, deferred data, authentication and Content Security Policy. These declarations are optional: components can still perform UI-local work and client-side fetching when that work does not belong to the server response contract.

## Undeclared routes stay client-rendered

Client-side rendering is not a third value of `attr.render`. A URL known to the client router but absent from `taujs.config.ts` stays client-rendered once the application shell has loaded.

For a direct request to such a URL, something must still serve that shell:

- a Fastify instance created by τjs supplies the application-shell fallback
- a Fastify instance supplied by the caller requires an explicit terminal `/*` τjs route when τjs should own unmatched document URLs

See [Request Contracts & Data](/guides/request-contracts/#undeclared-urls-and-client-routing), [Application Shell Pattern](/reference/app-shell-pattern/) and [Host Ownership](/guides/host-ownership/) for the full boundary.

## Request flow

1. Fastify matches the request to a declared τjs route.
2. τjs resolves route policy, head work and critical data.
3. The selected renderer produces an SSR or streaming response.
4. Deferred entries can continue behind native framework boundaries without delaying independent shell content.
5. Hydration starts from the server snapshot when the route enables it.

The [Architecture](/guides/architecture/) guide explains the ownership boundaries. [Data Loading](/guides/data-loading/) covers critical and deferred loaders.

## Fastify ownership

The scaffold lets τjs create Fastify, then calls `app.listen()` from the server entry. You can instead pass an existing Fastify instance to `createServer`. In that form, the caller owns the host and τjs installs its declared routes and facilities inside an encapsulated scope.

That distinction affects the application shell, host-wide CSP, request identity and not-found handling. Read [Host Ownership](/guides/host-ownership/) before embedding τjs into an existing server.

## Development and production

In development, Vite supplies framework transforms and HMR while the generated server process reloads as its TypeScript changes. τjs also exposes its development request graph and episode tooling.

In production, the application build contains its client assets and server-rendering modules. Fastify dispatches the registered routes and serves the configured static assets; the development recorder and Vite middleware are absent.

## Where to go next

- [Architecture](/guides/architecture/) for the system boundaries
- [Request Contracts & Data](/guides/request-contracts/) for what belongs in configuration
- [Incremental Migration](/guides/incremental-migration/) for adopting τjs inside an existing application
- [Host Ownership](/guides/host-ownership/) for supplying your own Fastify instance
