{
  "name": "rfc-0014-recipe",
  "version": "0.0.4",
  "private": true,
  "description": "RFC 0014 M6: the complexity-budget recipe (docs/rfcs/0014 §1), compiled against the PUBLISHED @taujs/server API - workspace-linked exports and generated declarations, not source paths.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc"
  },
  "dependencies": {
    "@taujs/server": "workspace:*",
    "fastify": "^5.8.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "typescript": "^5.5.4",
    "vitest": "^4.1.9"
  }
}
